# Live n8n pricing webhook — server-side only (read by /app/api/estimate/route.ts).
# Never expose this to the client bundle (do NOT prefix with NEXT_PUBLIC_).
N8N_WEBHOOK_URL=https://hu6j4d9e.rcld.app/webhook/squeegee-estimate
